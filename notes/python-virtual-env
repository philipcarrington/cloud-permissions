 -- Create the ENV
 python3 -m venv venv

 -- Activate the ENV:
source venv/bin/activate

-- Deactivate the ENV:
deactivate


sudo pip install virtualenv

#Dataflow requires python 3.7
virtualenv -p python3 venv

source venv/bin/activate
pip install apache-beam[gcp]ls